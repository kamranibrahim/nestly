rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Auth only — any signed-in user. No nest/role checks.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
