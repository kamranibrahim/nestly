rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Auth only — any signed-in user. No nest checks.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
